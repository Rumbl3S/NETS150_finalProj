Dataset source and layout
=========================

Full MovieLens 20M (recommended)
--------------------------------
1. Install Python deps (use the same interpreter as `python3`):  
   python3 -m pip install -r scripts/requirements-movielens.txt
2. From the project root run:  python3 scripts/download_movielens.py

This uses kagglehub to download **grouplens/movielens-20m-dataset** from Kaggle. Some Kaggle extracts use singular names (**movie.csv**, **rating.csv**); the script normalizes them to **movies.csv** and **ratings.csv** in:

  data/movielens-20m/

The Java app resolves datasets in this order:
  • Environment variable **MOVIELENS_20M_DIR** (absolute path to a folder that already contains movies.csv + ratings.csv), else
  • **<config>/movielens-20m/** under your config directory (default config is the `data` folder, so `data/movielens-20m/`), else
  • **<config>/movies.csv** and **<config>/ratings.csv** (small bundled sample) plus optional **movie_people.csv**.

Official GroupLens page (citation and other releases):
  https://grouplens.org/datasets/movielens/

Kaggle mirror used by the script:
  https://www.kaggle.com/datasets/grouplens/movielens-20m-dataset

Processing note
---------------
By default the download script writes only **1,000,000 rating rows** (plus header) into `ratings.csv` so disk and Java startup stay light. Override with environment variables on download: `MOVIELENS_RATINGS_MAX_LINES` (use `0` for full ~20M) and `MOVIELENS_RATINGS_ORDER` (`head` default, or `tail` for the last N rows of the file).

The Java app also stops after **1,000,000** rating lines when reading `ratings.csv` unless you pass `-Dmovielens.ratingLines=0` (full file) or another limit.

Each movie still keeps a **reservoir sample** (default 400 user ratings) from those rows for correlation. The graph is **sparse** (top similar neighbors per movie).

Cache
-----
After the first successful build, the graph is saved under **<dataset-folder>/cache/graph_cache.ser**. Delete that file to force a rebuild.
